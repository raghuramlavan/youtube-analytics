[project]
name = "youtube-analytics"
version = "0.1.0"
description = "youtube-analytics will download comments from youtube videos"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv==1.0.1",
    "requests==2.32.3",
]
